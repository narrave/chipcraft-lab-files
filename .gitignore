# Never commit plaintext Verilog — only encrypted files go to GitHub
*.v

# Encrypted Verilog files ARE tracked
!*.v.enc

# Student-created files in mywork/ are allowed as plain .v
!mywork/
!mywork/*.v

# Simulation outputs — generated locally, not committed
*.vcd
*.vvp
obj_dir/
