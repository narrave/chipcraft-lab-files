# Block everything by default — only .enc files may be committed
*

# Allow repo infrastructure
!.gitignore
!README.md

# Allow all folders and subfolders (so git looks inside them)
!*/
!**/*/

# Allow encrypted files anywhere in any subfolder
!*.enc
!**/*.enc
